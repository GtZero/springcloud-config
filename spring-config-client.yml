spring:
    profiles:
        active:
        - dev

---
server:
    port: 8201

spring:
    profiles: dev
    application:
        name: springcloudTotal-config-client

eureka:
    client:
        service-url:
            defaultZone: http://www.guotao.com:7001/eureka/
        
---
server:
    port: 8202

spring:
    profiles: test
    application:
        name: springcloudTotal-config-client

eureka:
    client:
        service-url:
            defaultZone: http://www.guotao.com:7001/eureka/   
#UTF8格式保存